[project]
name = "aegis-motion-prior"
version = "0.47.0"
description = "Aegis Motion V47 neural overlay-prior training and Aegis custom-data-asset JSON generation pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["motion-prior-service"]
