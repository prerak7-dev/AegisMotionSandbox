# Sample data

This folder contains a seed V37 JSON and one manifest.

The seed is not a trained AAA-quality result. It is included so the tensorization, retrieval, export, and service path can be tested immediately.

Replace the seed with real Manny/Quinn-retargeted animation clips to improve quality.
